{
  "name": "medilex",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "医療ドキュメントをRAGで検索するWebアプリケーション（モノレポ）",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "clean": "turbo run clean"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "turbo": "^2.0.0",
    "typescript": "^5.4.5"
  }
}
